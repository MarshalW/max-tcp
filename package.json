{
  "name": "max-tcp",
  "version": "1.1.3",
  "description": "",
  "main": "bin/app",
  "preferGlobal": true,
  "bin": {
    "max-tcp": "bin/app"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^4.0.1"
  }
}